# use-this-graph-to-build: Day04 梯度下降与模型评估 — 从导数到正则化
# confirm-sufficiency-before-start: 本图谱覆盖梯度下降全流程+评估指标+拟合诊断+正则化，6内容页布局
# scope: linear-regression/gradient-descent
# status: built

# ═══ Page 1: 正规方程 vs 梯度下降 — 什么时候换 ═══

选择: [正规方程O(n³+m·n²)-梯度下降O(m·n·k), 核心分界:特征数n>10000时正规方程矩阵求逆爆炸-梯度下降线性增长, n翻倍:正规方程计算量8倍-梯度下降仅2倍, feeds-变体:梯度下降有三种变体, critical]
选择.判断流程: [n>10000?->Yes->梯度下降SGDRegressor, No->需要在线学习?->Yes->梯度下降partial_fit, No->正规方程LinearRegression就够, 一句话:小特征正规方程-大特征梯度下降-10000维经验分界点, critical]
选择.特征少也能用: [梯度下降在特征少时一样能用-MSE凸函数全局最优只有一个-结果几乎相同, 但多了调参麻烦:学习率+迭代次数+标准化, 比喻:家离超市200米-走路3分钟到-开车也能到但还得找车位, critical]
选择.先试baseline: [工业界常见做法:先LinearRegression跑baseline, R²=0.95说明近线性关系-没必要折腾, R²=0.3说明线性不够-考虑换模型, 简单模型快可解释→看效果→不够再加复杂度, important]
变体: [三种梯度下降:区别仅在每步用多少样本算梯度, Batch:全部m个-方向准但慢, SGD:随机1个-快但抖, Mini-batch:随机64个-又快又稳-工业默认, critical]
变体.SGDRegressor: [sklearn用SGD训练的线性回归, SGDRegressor(max_iter=1000,eta0=0.01,learning_rate='invscaling'), 对比LinearRegression:后者用正规方程-无迭代, 没有单独的BatchGD或MiniBatchGD类-统一用SGDRegressor, important]

# ═══ Page 2: 梯度下降计算过程 — 正向传播与反向传播 ═══

计算过程: [每步都是加减乘除-没有脱离人类计算范畴, 正向传播:从w算到loss(左→右), 反向传播:从loss算回dw(右→左), 计算机只是帮你重复几百上千次, critical]
计算过程.正向: [ŷ=w·x+b算预测值, e=ŷ-y算误差, L=e²算损失, 具体:x=2-y=7-w=1-b=3->ŷ=5->e=-2->L=4, critical]
计算过程.反向: [链式法则:L不直接含w-一层层剥, ∂L/∂e=2e=-4, ∂e/∂z=1, ∂z/∂w=x=2, 乘起来:dw=-4×1×2=-8, db=-4×1×1=-4, 每层都是最基础求导:x²→2x|z-y→1|wx→x, critical]
计算过程.链式法则比喻: [L不认识w-但L认识e-e认识z-z认识w, 问L:e变了你怎么变→2e, 问e:z变了你怎么变→1, 问z:w变了你怎么变→x, 串起来:w变了L怎么变→2e×1×x, critical]
计算过程.多样本: [每个样本算一组dw和db-取平均, dw=1/m×Σ(ŷᵢ-yᵢ)×xᵢ, db=1/m×Σ(ŷᵢ-yᵢ), 误差大且特征值大的样本贡献最大, critical]
计算过程.多维推广: [n维:z=w₁x₁+w₂x₂+...+wₙxₙ+b, 每个dwⱼ=1/m×Σ(ŷᵢ-yᵢ)×xᵢⱼ, 矩阵写法:dw=1/m×Xᵀ@e-db=1/m×sum(e), numpy六行代码:n=3和n=100000一个字不用改, critical]
计算过程.自动减速: [误差变小→梯度变小→步子自动变小, 第1次迭代dw=-21.67→第2次dw=-0.377→缩小57倍, 越接近最优解走得越慢-天然防止冲过头, important]

# ═══ Page 3: 学习率与标准化 — 两个必调的旋钮 ═══

学习率: [α控制每步走多远:w=w-α×dw, 同样dw=-8:α=0.01变化0.08|α=0.1变化0.8|α=1.0变化8.0|α=10变化80飞出去, 不影响梯度方向-只影响每步走多远, critical]
学习率.诊断: [看loss曲线:稳步下降走平→合适, 下降极慢→太小→乘以3~10, 震荡上升爆炸→太大→除以3~10, 调参:0.001→0.003→0.01→0.03→0.1→0.3→1.0每次乘约3倍, critical]
学习率.与评估关系: [学习率合适→loss正常下降→w和b走到最优→测试集R²高, 学习率不影响模型能力上限-只影响能不能达到上限, 先调α→看loss曲线正常收敛→再看评估指标才有意义, critical]
标准化: [梯度=误差×特征值-特征值大的方向梯度大步子大, x₁范围1~5-x₂范围10000~50000→dw₂比dw₁大一万倍, 同一学习率:w₁几乎没动-w₂飞出去→震荡不收敛, 标准化后:各方向步子差不多→直奔谷底, critical]
标准化.操作: [StandardScaler-fit_transform训练集-transform测试集, 正规方程不需要标准化(直接解方程不受量级影响), 梯度下降必须标准化(各方向走得一样快), 标准化本身操作完全一样-区别在于需不需要, critical]
代码对比: [正规方程:LinearRegression().fit(X_train,y_train)-不需要标准化-无超参数, 梯度下降:多StandardScaler+换SGDRegressor+多max_iter和eta0, 核心区别三处:多标准化|换模型类|要调参, 结果几乎相同:R²差距<0.01, important]

# ═══ Page 4: 评估指标 — R² + RMSE 双视角 ═══

评估指标: [评估不是评估w和b本身-是评估预测值ŷ和真实值y的差距, 训练好的w和b代入公式-在测试集上算, 测试数据训练时从没见过-反映泛化能力, critical]
MSE: [1/m×Σ(ŷᵢ-yᵢ)²-平方放大大误差-单位是y²不直观, 梯度下降的损失函数就是它-训练和评估用同一个, critical]
RMSE: [√MSE-开根号单位回到y-"平均偏差约3万"好解释, 实际汇报最常用-还是对大误差敏感(骨子里还是MSE), critical]
MAE: [1/m×Σ|ŷᵢ-yᵢ|-不平方-大误差不被放大, 数据有异常值时比MSE更稳, RMSE比MAE大很多→说明有少数离谱预测, important]
R²: [R²=1-Σ(ŷᵢ-yᵢ)²/Σ(ȳ-yᵢ)²-比"无脑猜平均值"好多少, R²=1完美|0.95消除95%误差|0跟猜平均值一样|<0比猜还差, 唯一有标准刻度的指标-不受单位影响-不同数据集可比, critical]
评估组合: [默认R²+RMSE:R²定性判断好不好-RMSE定量感受偏差多大, 有异常值加MAE:跟RMSE对比判断异常影响, MSE基本不单独看-RMSE=√MSE看RMSE就行, critical]
评估方法: [留出法hold-out:80%训练20%测试-简单快速, K折交叉验证:数据分K=5份-轮流做测试集-评估更稳, 数据量大→留出法够-数据量小→交叉验证更可信, important]

# ═══ Page 5: 拟合诊断与正则化 — 欠拟合·过拟合·L1·L2 ═══

拟合诊断: [比较训练集R²和测试集R², 都低→欠拟合(模型太简单), 都高且接近→正好, 训练高测试低→过拟合(模型太复杂), critical]
欠拟合: [模型太简单学不到规律, 操作:加多项式PolynomialFeatures|加特征|换更复杂模型|减小正则化强度, loss降到底了但还是很高→不是训练问题-是模型选择问题, critical]
过拟合: [模型太复杂把噪声也当规律学了, 特征:参数w太大-模型为贴合每个点w变极端, 正常:w=[2.1,0.8,1.0]-过拟合:w=[150,-230,85], 操作:加正则化|减特征|加数据量|换简单模型, critical]
正则化: [在loss里加惩罚项-w越大惩罚越重, L=MSE+λ×(w的大小), λ控制惩罚力度:λ=0无惩罚→可能过拟合|λ适中→刚好|λ太大→欠拟合, 比喻:地心引力-误差推着w往上-正则化拉着w往下-最终在平衡点稳定, critical]
正则化.介入点: [只介入两处:loss多了λ×Σwⱼ²→dw多了λ×w, 正向传播不变|更新公式不变|训练完之后不参与, loss改了→求导自然多一项→一个因导致一个果, critical]
L2-Ridge: [L=MSE+λ×Σwⱼ²-惩罚w的平方-求导得λ×2w, 力跟w成正比(弹簧):w大拉力大-w小拉力小→永远不到0, 效果:全部变小但都保留, 适合:特征都有用-只是怕过拟合, Ridge(alpha=1.0), critical]
L1-Lasso: [L=MSE+λ×Σ|wⱼ|-惩罚绝对值-求导得λ×sign(w), 力是固定±1(刀):不管w多大多小每次砍固定量→小的w直接归0, 效果:没用的w归零=自动特征选择, 适合:怀疑有些特征是噪声, Lasso(alpha=0.1), critical]
L2vsL1: [L2弹簧拉所有人-L1刀砍弱者, 特征都有用→Ridge|特征有垃圾→Lasso|不确定→ElasticNet混合, 标准化不能治异常值:标准化管特征间尺度差异-异常值管样本间偏离程度, critical]

# ═══ Page 6: 章节小结 ═══

小结.核心知识: [1-正规方程vs梯度下降:n>10000分界线, 2-计算过程:正向传播→反向传播(链式法则)→更新参数, 3-学习率:看loss曲线调-太大震荡太小龟速, 4-标准化:梯度下降必须做-各方向步长均匀, 5-评估:R²+RMSE双视角-R²定性RMSE定量, 6-拟合:训练集vs测试集R²判断, 7-正则化:L2弹簧压所有-L1刀砍弱者, critical]
小结.易错清单: [loss曲线管训练过程对不对-R²管模型选择对不对-两个层面不能混, 标准化治不了异常值-两个不同层面问题, 一次方程调到死也达不到三次的效果-这是模型能力上限不是训练问题, R²不是对平均值求差-是你的模型误差vs猜平均值误差的比, critical]
小结.day05预告: [逻辑回归:从回归到分类, 交叉熵损失函数, sigmoid函数, 分类评估指标:准确率|精确率|召回率|F1, critical]

# ~95%
